# Ralph Progress Log
Started: Mon 23 Mar 2026 13:00:27 CET
---

## Codebase Patterns
- Backend venv: `backend/.venv/bin/python`, needs `DJANGO_SECRET_KEY=dev-secret`
- Need `whitenoise` installed in venv for tests to pass
- Migrations numbered sequentially in `core/migrations/`
- Data migrations use `apps.get_model()` pattern with forward + reverse functions
- Tests in `core/tests/test_*.py`, use `APIClient` + `force_login`
---

## 2026-03-23 - US-001
- Created `ReceiptImage` model (FK to Expense, related_name='receipts', ImageField upload_to='receipts/', auto_now_add created_at)
- Generated schema migration `0011_receiptimage.py`
- Created data migration `0012_migrate_receipt_images.py` to copy existing `receipt_image` paths
- Added tests in `test_models.py`: creation and cascade delete
- Files changed: `core/models.py`, `core/migrations/0011_receiptimage.py`, `core/migrations/0012_migrate_receipt_images.py`, `core/tests/test_models.py`
- **Learnings for future iterations:**
  - `python` not available, use `.venv/bin/python` with `DJANGO_SECRET_KEY=dev-secret`
  - `whitenoise` missing from venv, needed manual install
  - All 90 tests pass
---

## 2026-03-23 - US-002
- Created `ExpenseReceiptView` with POST handler: validates creator ownership (403), file presence (400), size max 5MB (400), max 5 receipts (400), returns 201 with {id, image}
- Added URL pattern `expenses/<int:pk>/receipts/` in `core/urls.py`
- Added 6 tests in `core/tests/test_receipts.py`: success, no image, non-creator 403, max 5 limit, too large, nonexistent expense
- Files changed: `core/views.py`, `core/urls.py`, `core/tests/test_receipts.py`
- **Learnings for future iterations:**
  - `MultiPartParser` already imported in views.py (used by MeAvatarView)
  - Use `Expense.objects.get()` (not `all_objects`) so deleted expenses can't get receipts
  - All 96 tests pass
---

## 2026-03-23 - US-003
- Added DELETE handler to `ExpenseReceiptView`: validates creator ownership (403), receipt belongs to expense (404), returns 204 on success
- Added URL pattern `expenses/<int:pk>/receipts/<int:receipt_id>/` in `core/urls.py`
- Added 4 tests in `ReceiptDeleteTest`: success, non-creator 403, receipt not on expense 404, nonexistent expense 404
- Files changed: `core/views.py`, `core/urls.py`, `core/tests/test_receipts.py`
- **Learnings for future iterations:**
  - DELETE method on same view class, URL adds `<int:receipt_id>` param — Django routes to same view, method dispatch handles it
  - All 100 tests pass
---

## 2026-03-23 - US-004
- Added `ReceiptImageSerializer` (read-only: id, image) to `core/serializers.py`
- Added `receipts = ReceiptImageSerializer(many=True, read_only=True)` to `ExpenseSerializer`
- Old `receipt_image` field kept for backwards compatibility
- Added 3 tests in `ReceiptSerializerTest`: receipts array in list, empty receipts, receipt_image field still present
- Files changed: `core/serializers.py`, `core/tests/test_receipts.py`
- **Learnings for future iterations:**
  - `related_name='receipts'` on ReceiptImage FK means DRF nested serializer works automatically
  - All 103 tests pass
---

## 2026-03-23 - US-005
- Added Receipts section to EditExpenseForm.vue below form fields, above "Replace receipt image"
- Shows thumbnails (max 80px height, aspect ratio preserved via object-fit: contain) from expense.receipts array
- Shows "No receipts attached" placeholder when array empty
- Added `receipts` ref initialized from `props.expense.receipts`
- Files changed: `frontend/src/components/EditExpenseForm.vue`
- **Learnings for future iterations:**
  - expense.receipts is `Array<{id, image}>` from ReceiptImageSerializer (US-004)
  - Thumbnails use `cursor: pointer` anticipating US-006 click-to-view
  - All 103 backend tests pass, typecheck passes
---

## 2026-03-23 - US-006
- Added full-size receipt viewer overlay to EditExpenseForm.vue
- Clicking a thumbnail sets `viewingReceipt` ref, opening a fixed overlay (z-index 200) with dark background
- Close by clicking background or X button; image click stopped from propagating
- Reused same CSS pattern from GroupDetailView's receipt-overlay
- Files changed: `frontend/src/components/EditExpenseForm.vue`
- **Learnings for future iterations:**
  - Receipt overlay placed inside modal-overlay but with higher z-index (200 vs 100) works fine
  - `@click.self` on modal-overlay doesn't fire for clicks on child overlay elements
  - All 103 backend tests pass, typecheck passes
---

## 2026-03-23 - US-007
- Added delete button (X) on each receipt thumbnail, visible only to expense creator
- Clicking delete shows `confirm()` prompt, then calls `DELETE /api/expenses/{id}/receipts/{receipt_id}/`
- Thumbnail removed from list on success; error shown on failure
- Added `deleteReceipt` method to `api.ts`
- Wrapped thumbnails in `receipt-thumb-wrap` div for relative positioning of delete button
- Files changed: `frontend/src/components/EditExpenseForm.vue`, `frontend/src/api.ts`
- **Learnings for future iterations:**
  - `expense.created_by` (user id) available on expense object, compare with `auth.user?.id` for creator check
  - `confirm()` is simplest confirmation approach for destructive actions in modals
  - All 103 backend tests pass, typecheck passes
---

## 2026-03-23 - US-008
- Added `uploadReceipt` method to `api.ts` using FormData POST to `/api/expenses/{id}/receipts/`
- Replaced old "Replace receipt image" file input with inline upload input below receipt thumbnails
- File input hidden when 5 receipts attached or user is not the creator
- New thumbnail appended to list on successful upload; input reset after each upload
- Removed old `receiptFile` ref and `onFile` handler; removed legacy FormData PATCH upload from submit
- Files changed: `frontend/src/components/EditExpenseForm.vue`, `frontend/src/api.ts`
- **Learnings for future iterations:**
  - `api.uploadReceipt` uses raw `fetch` (like `uploadAvatar`) since it's multipart FormData, not JSON
  - Reset `input.value = ''` after upload so same file can be re-selected
  - All 103 backend tests pass, typecheck passes
---

## 2026-03-23 - US-009
- Replaced old PATCH `/api/expenses/{id}/` with `receipt_image` FormData upload with `api.uploadReceipt(expense.id, file)` which POSTs to `/api/expenses/{id}/receipts/`
- Kept existing file input in add expense form
- Removed 8 lines of manual fetch code, replaced with 1-line API call
- Files changed: `frontend/src/components/AddExpenseForm.vue`
- **Learnings for future iterations:**
  - `api.uploadReceipt` already existed from US-008, just needed to wire it into the create flow
  - All 103 backend tests pass, typecheck passes
---
