# Ralph Progress Log
Started: Sun 22 Mar 2026 18:54:54 CET
---

## Codebase Patterns

- **Breakpoint variables**: `--bp-phone: 768px`, `--bp-tablet: 1024px` defined in `base.css` — use in media queries across all views
- **Table scroll**: Wrap tables in `.table-scroll-container` for horizontal scroll on small screens
- **box-sizing**: Already global `border-box` in `base.css` (lines 53-56)
- **Viewport meta**: Already in `index.html`
- **Typecheck**: Run `npx vue-tsc --build` from `frontend/` (need `npm install` first if node_modules missing)
- **main.css**: Cleaned up — removed default Vue template grid/padding; `#app` now only sets `font-weight: normal`
- **AppNavbar integration**: Import `AppNavbar` from `@/components/AppNavbar.vue` and use `<AppNavbar />` at top of view template, removing inline navbar markup and related CSS/JS
- **Responsive visibility**: Use `.desktop-only` / `.mobile-only` classes with `@media (max-width: 1023px)` and `@media (min-width: 1024px)` — matches `--bp-tablet: 1024px`

---

## 2026-03-22 - US-001
- Added CSS custom properties `--bp-phone: 768px` and `--bp-tablet: 1024px` to `:root` in `base.css`
- Added `.table-scroll-container` utility class (overflow-x auto, width 100%) to `base.css`
- Confirmed `box-sizing: border-box` already present globally
- Confirmed viewport meta tag already in `index.html`
- Files changed: `frontend/src/assets/base.css`
- **Learnings for future iterations:**
  - `base.css` already has comprehensive reset including box-sizing
  - `main.css` has Vue template default styles (grid layout at 1024px) that will likely need modification for navbar integration (US-004)
  - `npm install` needed before typecheck if fresh checkout
---

## 2026-03-22 - US-002
- Created `frontend/src/components/AppNavbar.vue` — desktop horizontal navbar
- Brand "MemoryBank" on left, nav links (Dashboard, Profile, Admin if staff) center-right, Sign out far right
- Active route highlighted via `exact-active-class` (Dashboard) and `active-class` (others)
- Uses auth store for user info, isStaff check, logout action
- Uses Vue Router for navigation and active route detection
- Files changed: `frontend/src/components/AppNavbar.vue`
- **Learnings for future iterations:**
  - Dashboard link needs `exact-active-class` not `active-class` since `/` would match all routes
  - Existing views all have inline `.navbar` CSS — US-004 will need to remove these and import AppNavbar
  - Navbar pattern: flex with gap, brand left, links auto-margin-left, logout margin-left
---

## 2026-03-22 - US-003
- Added hamburger menu to AppNavbar for screens below 1024px
- Desktop nav links/logout hidden on mobile, replaced by hamburger button
- Hamburger animates to X via CSS transforms on the 3 lines
- Dropdown menu with all nav links + sign out appears below navbar
- Click-outside-to-close via document click listener + overlay backdrop
- Menu closes on link click
- 44px min touch target on hamburger button
- Files changed: `frontend/src/components/AppNavbar.vue`
- **Learnings for future iterations:**
  - Use `@click.stop` on hamburger and mobile menu to prevent document click handler from immediately closing
  - CSS approach: `.desktop-only` / `.mobile-only` classes with media queries + `!important` to override scoped display values
  - Hamburger animation: 3 spans, middle fades out, top/bottom rotate ±45deg with translateY to meet at center
  - Overlay div must be outside the `<header>` to cover the full viewport
---

## 2026-03-22 - US-003 committed
- Commit: d034ada
---

## 2026-03-22 - US-004
- Replaced inline navbar markup with `<AppNavbar />` in DashboardView, GroupDetailView, ProfileView, AdminView
- Removed per-view navbar CSS and related JS (handleLogout, unused router imports)
- LoginView left unchanged (no navbar on login page)
- Cleaned up `main.css` — removed default Vue template grid layout and padding that constrained `#app` width
- GroupDetailView: moved group name into an `<h2>` inside content since navbar no longer shows it
- Files changed: `DashboardView.vue`, `GroupDetailView.vue`, `ProfileView.vue`, `AdminView.vue`, `main.css`
- **Learnings for future iterations:**
  - `main.css` had default Vue template styles (`max-width: 1280px`, grid layout at 1024px) that broke full-width navbar — always check for inherited layout constraints
  - When removing inline navbars, also remove related imports (RouterLink if unused, useRouter if only used for logout)
  - GroupDetailView had the group name in the old navbar — needed to relocate it to content area
---
