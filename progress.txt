# Ralph Progress Log
Started: Sun 22 Mar 2026 20:08:44 CET
---

## Codebase Patterns
- Python venv at `backend/.venv/` — run tests with `.venv/bin/python manage.py test core`
- Permission classes live in `backend/core/permissions.py`, views import from there
- Existing `AdminWritePermission` in `views.py` — new permissions go in `permissions.py`
- Frontend CSP meta tag in `index.html` — update if new asset sources needed
- django-csp v4 uses `CONTENT_SECURITY_POLICY` dict with `DIRECTIVES` sub-dict, values are lists of strings
- HTTPS/SSL settings gated behind `DJANGO_SECURE_SSL` env var pattern
---

## 2026-03-23 - #7 Harden Django cookie & HTTPS security settings
- Added cookie security (CSRF_COOKIE_HTTPONLY, SAMESITE, SESSION_COOKIE_HTTPONLY, SAMESITE)
- Added HTTPS settings gated behind `DJANGO_SECURE_SSL` env var
- Changed ALLOWED_HOSTS default from `*` to `localhost,127.0.0.1`
- Added DJANGO_SECURE_SSL to docker-compose.yml and docker-compose.ghcr.yml
- Files changed: `backend/memorybank/settings.py`, `docker-compose.yml`, `docker-compose.ghcr.yml`
- **Learnings for future iterations:**
  - Always gate SSL/HTTPS settings behind env var so local dev and tests work without TLS
  - 71 tests pass
---

## 2026-03-23 - #8 Add input length & content validation to serializers
- Added explicit `max_length` to all text serializer fields
- Added explicit `max_digits=12, decimal_places=2` to all DecimalFields
- Added whitespace stripping via `validate_<field>` methods
- Added control character rejection for expense description
- Files changed: `backend/core/serializers.py`
- **Learnings for future iterations:**
  - DRF ModelSerializer inherits model constraints but explicit serializer-level fields override them
  - 71 tests pass
---

## 2026-03-23 - #9 Add Content-Security-Policy and security headers via Django
- Added `django-csp>=4.0` to requirements.txt, `csp.middleware.CSPMiddleware` to MIDDLEWARE
- Configured CONTENT_SECURITY_POLICY with all directives
- Set explicit X_FRAME_OPTIONS='DENY' and SECURE_CONTENT_TYPE_NOSNIFF=True
- Files changed: `backend/requirements.txt`, `backend/memorybank/settings.py`
- **Learnings for future iterations:**
  - django-csp v4 uses dict-based config, not the old `CSP_*` settings
  - 71 tests pass
---

## 2026-03-23 - #10 Add CSP meta tag to frontend index.html as fallback
- Added `<meta http-equiv="Content-Security-Policy">` to `frontend/index.html` with directives: default-src 'self', script-src 'self', style-src 'self' 'unsafe-inline', img-src 'self' data:, font-src 'self', connect-src 'self', frame-ancestors 'none'
- Files changed: `frontend/index.html`
- **Learnings for future iterations:**
  - Single-line change, no build/typecheck impact
  - 71 backend tests pass on this branch
---

## 2026-03-22 - US-009
- Added 4 validation tests to `backend/core/tests/test_security.py`
- Tests: settlement with non-member payee → 400, expense with non-member split → 400
- Tests: settlement with valid payee → 201, expense with valid splits → 201
- Files changed: `backend/core/tests/test_security.py`
- Full suite: 71 tests pass
---

## 2026-03-22 - US-008
- Created `backend/core/tests/test_security.py` with 8 authorization tests
- Tests: bob (non-member) gets 403 on all 6 group endpoints (GET/POST expenses, GET/POST settlements, GET balances, GET export)
- Tests: staff non-member gets 200, regular member gets 200
- Files changed: `backend/core/tests/test_security.py` (new)
---

## 2026-03-22 - US-007
- Removed hardcoded SECRET_KEY fallback; raises RuntimeError if DJANGO_SECRET_KEY not set
- Test runner gets a safe fallback via `'test' in sys.argv` check
- Changed DEBUG default from 'true' to 'false'
- Updated docker-compose.yml and docker-compose.ghcr.yml with DJANGO_SECRET_KEY and DJANGO_DEBUG=false
- Files changed: `backend/memorybank/settings.py`, `docker-compose.yml`, `docker-compose.ghcr.yml`
---

## 2026-03-22 - US-006
- Added group membership validation for split user_ids in `ExpenseSerializer._create_splits()`
- Checks all user_ids in split_data against `members.values_list('id', flat=True)`
- Returns 400 with descriptive error listing non-member user IDs
- Equal splits (no split_data) unaffected — already uses group members
- Files changed: `backend/core/serializers.py`
---

## 2026-03-22 - US-005
- Added payee group membership validation in `GroupSettlementView.post()`
- Checks `group.members.filter(pk=payee_id).exists()` before saving
- Returns 400 with descriptive error if payee is not a group member
- Files changed: `backend/core/views.py`
- **Learnings for future iterations:**
  - `SettlementSerializer` has `payee` as a writable field — validated_data contains the User object
---

## 2026-03-22 - US-004
- Applied `IsGroupMemberOrAdmin` permission to `GroupBalanceView` and `GroupExportView`
- Files changed: `backend/core/views.py`
---

## 2026-03-22 - US-003
- Applied `IsGroupMemberOrAdmin` permission to `GroupSettlementView`
- Files changed: `backend/core/views.py`
---

## 2026-03-22 - US-002
- Applied `IsGroupMemberOrAdmin` permission to `GroupExpenseView`
- Files changed: `backend/core/views.py`
- **Learnings for future iterations:**
  - Just add `permission_classes = [IsGroupMemberOrAdmin]` and import — minimal change
  - All 59 tests still pass (existing tests use group members or staff)
---

## 2026-03-22 - US-001
- Created `IsGroupMemberOrAdmin` permission class in `backend/core/permissions.py`
- Extracts group pk from URL kwargs, checks membership via `group.members.filter(pk=user.pk).exists()`
- Staff users bypass membership check
- Returns False for non-members, non-staff
- Files changed: `backend/core/permissions.py` (new)
- **Learnings for future iterations:**
  - No venv existed initially — created at `backend/.venv/`
  - All 59 existing tests pass with the new permission class (no views use it yet)
---
